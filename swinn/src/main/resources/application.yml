spring:
  datasource:
    url: jdbc:mysql://localhost:3306/swim_db?serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&useSSL=false
    username: swim_user
    password: qwer1234!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true